org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official
# AGP 9 enables built-in Kotlin/new DSL by default. This foundation intentionally
# pins Kotlin 2.3.21 + Compose compiler together, so temporarily opt out until the
# repository is migrated as one tested change.
android.builtInKotlin=false
android.newDsl=false
